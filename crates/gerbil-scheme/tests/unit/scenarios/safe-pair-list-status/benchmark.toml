harness = "libtest"
test = "scheme_pair_list_status_wrappers_fail_closed_for_unbacked_handles"
phase = "warm"
observed_total = "100us"
target_total = "1ms"
target_rationale = "Safe status wrappers are direct ABI calls plus NativeResult projection; unbacked fail-closed checks must stay below one millisecond in warm local runs."
max_total = "500ms"
regression_budget = "10ms"
observed_memory_bytes = 0
memory_budget_bytes = 16777216

[observed_timings]
safe_pair_list_status = "100us"
safe_pair_list_status_budget = "1ms"
