harness = "libtest"
test = "discovers_gambit_library_from_a_gerbil_wrapper"
phase = "warm"
target_total = "10ms"
max_total = "500ms"
observed_total = "2ms"
regression_budget = "50ms"
memory_budget_bytes = 67108864
observed_memory_bytes = 0
target_rationale = "The gate materializes a fake wrapper and matching Gambit library, then resolves the link search path without invoking a compiler."

[observed_timings]
wrapper_discovery = "2ms"
wrapper_discovery_budget = "50ms"
