harness = "libtest"
test = "stable_status_codes_round_trip_without_transmute"
phase = "warm"
target_total = "10ms"
max_total = "100ms"
observed_total = "5ms"
regression_budget = "50ms"
memory_budget_bytes = 16777216
observed_memory_bytes = 0
target_rationale = "Status decoding is a constant-time match that must remain allocation free and preserve unknown future codes."

[observed_timings]
status_projection = "5ms"
