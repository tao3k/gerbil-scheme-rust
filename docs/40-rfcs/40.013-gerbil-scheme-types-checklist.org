#+TITLE: RFC 40.013 Gerbil Scheme Types Binding Checklist
#+DATE: 2026-07-20
#+STATUS: Draft

Bridge engineering rules for all checklist items live in
[[file:40.014-gerbil-scheme-rust-bridge-engineering-standard.org][RFC 40.014]].

* Purpose

This checklist tracks the remaining work required for =gerbil-scheme-rust= to
become a general-purpose Gerbil Scheme Rust bindings library, rather than a
Marlin-specific extraction.

The checklist is intentionally type-first. Marlin integration should consume
this crate only after the native Scheme value surface, ownership rules,
scenario coverage, and performance receipts are clear enough to be reused.

* Current evidence baseline

Current Rust ABI evidence:

- =gerbil-scheme-sys= exposes =GerbilStatus=, =GerbilBorrowedUtf8=,
  =GerbilRuntimeOpaque=, =GerbilValueHandle=, and =GerbilI64Callback=.
- =gerbil-scheme= exposes safe =GerbilRuntime=, =GerbilUtf8=,
  =GerbilRuntimeReceipt=, =GerbilValue=, =GerbilI64Callback=,
  =GerbilI64CallbackAbi=, and =NativeError=.
- Scalar native operations currently cover i64 identity/addition, i64 parity,
  and i64 comparison.
- =native-value-surface= scenario covers safe UTF-8, opaque value handles, and
  callback trampoline behavior.
- =source_surface= integration tests cover the Scheme ASP source-surface export
  set, the tracked =scheme/native.ssi= contract, and the rule that
  =scheme/asp/native-surface.ss= stays out of the runtime build.

Current Scheme source-surface evidence:

- =scheme/native.ss= remains the runtime implementation owner.
- =scheme/native.ssi= is the tracked Gerbil signature contract for exported
  bridge symbols.
- =scheme/asp/native-surface.ss= is still the temporary ASP-projected
  workaround for the source-surface view; it must stay out of =build.ss= and can
  be retired once the provider projects =scheme/native.ss= plus
  =scheme/native.ssi= directly.
- The current projected selector set is exactly 21 selectors:
  =gerbil_scheme_rust_abi_version=, =gerbil_scheme_rust_runtime_init=,
  =gerbil_scheme_rust_runtime_cleanup=, =gerbil_scheme_rust_identity_i64=,
  =gerbil_scheme_rust_add_i64=, =gerbil_scheme_rust_is_even_i64=,
  =gerbil_scheme_rust_compare_i64=,
  =gerbil_scheme_rust_runtime_handle_shape=,
  =gerbil_scheme_rust_status_shape=, =gerbil_scheme_rust_i64_shape=,
  =gerbil_scheme_rust_bool_shape=,
  =gerbil_scheme_rust_comparison_shape=,
  =gerbil_scheme_rust_fixnum_shape=,
  =gerbil_scheme_rust_char_shape=,
  =gerbil_scheme_rust_flonum_shape=,
  =gerbil_scheme_rust_utf8_shape=,
  =gerbil_scheme_rust_value_handle_shape=,
  =gerbil_scheme_rust_i64_callback_shape=,
  =gerbil_scheme_rust_native_value_shape=,
  =gerbil_scheme_rust_native_error_shape=, and
  =gerbil_scheme_rust_native_result_shape=.
- Direct provider projection of the real =scheme/native.ss= FFI/export owner is
  not yet closed and remains a provider/source-surface TODO.
- No current Scheme selector exists for nil, void, symbol, keyword, pair,
  list, vector, procedure, owned/rooted values, bytevectors, ports,
  continuations, or advanced Scheme objects.

* Checklist status legend

- =[X]= implemented and covered by tests or scenario receipts.
- =[ ]= not implemented or not accepted.
- =[?]= partially implemented; owner or semantics still need proof.

* Public backed type matrix

This matrix is the current release-auditable v0 binding surface. A type family
is accepted only when the raw ABI, safe Rust surface, Scheme native-surface
selector, ownership/nullability rule, failure policy, and scenario coverage are
all present. The Rust test =public_backed_type_matrix_covers_current_native_surface=
keeps this table aligned with the Scheme source-surface projection.

| Family | Scheme selector | Raw ABI | Safe Rust surface | Ownership/rooting | Nullability | Failure policy | Scenario |
| status | =gerbil_scheme_rust_status_shape= | =GerbilStatus= | =NativeError::status= | copy status code | not pointer-backed | unknown status preserves code | =status-contract= |
| runtime lifecycle | =gerbil_scheme_rust_runtime_handle_shape= | process-global native runtime init/cleanup | =GerbilRuntime= and =GerbilRuntimeReceipt= | one-shot process-global owner, thread-affine safe handle | not pointer-backed | live re-entry returns =AlreadyInitialized=; finalized runtime returns =RuntimeFinalized=; wrong-thread is blocked by =!Send=/=!Sync= and defensively checked | =native-runtime-round-trip= |
| i64 | =gerbil_scheme_rust_i64_shape= | =i64= | =GerbilRuntime::{identity_i64,add_i64}= | copy scalar | not pointer-backed | overflow maps to =IntegerOverflow= | =native-identity-round-trip= |
| bool | =gerbil_scheme_rust_bool_shape= | =GerbilStatus= plus Rust =bool= return plus Scheme boolean handle | =GerbilRuntime::is_even_i64= and =GerbilValue::as_boolean= | copy scalar or runtime-borrowed Scheme object | not pointer-backed | status fail-closed before bool projection; raw provenance rejected | =native-runtime-round-trip= |
| comparison | =gerbil_scheme_rust_comparison_shape= | =i32= comparison result | =GerbilRuntime::compare_i64= | copy scalar | not pointer-backed | invalid result maps to =InvalidComparisonResult= | =invalid-comparison-fail-closed= |
| fixnum | =gerbil_scheme_rust_fixnum_shape= | =GerbilFixnum= | =GerbilValue::{is_fixnum,as_fixnum,as_fixnum_i64}= | runtime-borrowed Scheme object | not pointer-backed | non-fixnum and raw provenance fail closed | =native-runtime-round-trip= |
| char | =gerbil_scheme_rust_char_shape= | =GerbilChar= | =GerbilValue::{is_char,as_char}= | runtime-borrowed Scheme object | not pointer-backed | invalid Unicode scalar and raw provenance fail closed | =native-runtime-round-trip= |
| flonum | =gerbil_scheme_rust_flonum_shape= | =GerbilFlonum= | =GerbilValue::{is_flonum,as_flonum}= | runtime-borrowed Scheme object | not pointer-backed | non-flonum and raw provenance fail closed | =native-runtime-round-trip= |
| borrowed UTF-8 | =gerbil_scheme_rust_utf8_shape= | =GerbilBorrowedUtf8= | =GerbilUtf8= | borrowed Rust UTF-8 bytes | empty string may use null pointer with zero length | non-UTF-8 is reserved for future bytevector surface | =native-value-surface= |
| opaque value handle | =gerbil_scheme_rust_value_handle_shape= | =GerbilValueHandle= | =GerbilValue<'runtime>= | runtime-borrowed word-sized opaque handle, not rooted | zero rejected before FFI crossing | zero maps to =NullPointer= status | =backed-value-family-surface= |
| i64 callback | =gerbil_scheme_rust_i64_callback_shape= | =GerbilI64Callback= | =GerbilI64Callback= and =GerbilI64CallbackAbi= | borrowed callback/context pair | null context rejected before Rust call | panic contained at native boundary | =native-value-surface= |
| native value | =gerbil_scheme_rust_native_value_shape= | current backed C ABI value families | =GerbilRuntime=, =GerbilUtf8=, =GerbilValue=, and =GerbilI64Callback= | aggregate of scalar, borrowed, handle, and callback shapes | explicit per concrete shape | delegated to concrete backed value family | =backed-value-family-surface= |
| native error | =gerbil_scheme_rust_native_error_shape= | =GerbilStatus= | =NativeError= | Rust safe-boundary error enum | optional status projection | unknown status preserves code | =source-surface-sync= |
| native result | =gerbil_scheme_rust_native_result_shape= | =GerbilStatus= plus native value payload | =Result<T, NativeError>= | ok native value or native error | delegated to value/error shape | fail-closed | =source-surface-sync= |

* Type surface checklist

** Runtime and lifecycle

- [X] Process-global runtime safe owner: =GerbilRuntime=.
- [X] Opaque value handles carry explicit provenance:
  =GerbilValueProvenance::UntrustedRaw= for caller-supplied non-zero raw
  handles.
- [X] Real Scheme object export handles carry explicit provenance:
  =GerbilValueProvenance::SchemeObjectExport= for borrowed/unrooted values
  produced by the initialized Gerbil native module.
- [X] Runtime-sentinel opaque value provenance is distinct from =UntrustedRaw=.
- [ ] Runtime/export-produced opaque value provenance that can safely enable
  Gerbil/Gambit predicates and traversal.
- [X] Thread-affinity check for runtime methods.
- [X] ABI version check before exposing runtime handle.
- [X] Drop-time native cleanup path.
- [X] Explicit runtime re-entry policy document: what is legal after cleanup,
  what is process-final, and what must fail closed.
- [X] Scenario for live re-entry and runtime-finalized behavior.
- [ ] Compile-time or defensive scenario for wrong-thread access. Safe
  =GerbilRuntime= is intentionally =!Send=/=!Sync=, so a runtime wrong-thread
  scenario must not weaken the public API just to make the branch reachable.
- [X] Public receipt type for runtime initialization, ABI id, ABI version, and
  native module path.

** Status, error, and result

- [X] Stable raw status enum: =GerbilStatus=.
- [X] Safe native error enum: =NativeError=.
- [X] Unknown status preservation in status contract tests.
- [X] Panic containment for i64 callback trampoline.
- [ ] Public =GerbilResult<T>= alias or typed result wrapper.
- [?] Scheme-side result shape projection for success/error payloads is present
  as ASP source-surface taxonomy, but no public =GerbilResult<T>= wrapper exists
  yet.
- [ ] Error mapping table from Gerbil exception classes to stable Rust errors.
- [X] Rust scenario/test receipt that validates status/error/result projection
  together, including unknown status preservation and optional status
  projection.

** Nil, void, eof, and unspecified

- [ ] Raw ABI representation for =#<void>=.
- [ ] Raw ABI representation for =()= / nil.
- [ ] Raw ABI representation for EOF object if exported.
- [ ] Raw ABI representation for unspecified values.
- [ ] Safe Rust marker types for nil/void/eof/unspecified.
- [ ] Tests proving these sentinels are distinguishable and do not alias null.

** Boolean

- [X] Current scalar bool use through i64 parity operation.
- [X] Scheme ASP surface contains a boolean shape entry, and safe first-class
  boolean value projection is complete for =SchemeObjectExport= handles.
- [X] Raw ABI shape for Scheme boolean values as values, not only Rust bool
  return.
- [X] Safe direct =bool= projection rules through =GerbilValue::as_boolean=.
- [X] Scenario for boolean round-trip through runtime-exported =#t= and =#f=
  fixtures.
- [X] Scenario for invalid boolean raw values beyond provenance fail-closed
  coverage.

** Exact integers and machine integers

- [X] i64 identity native round-trip.
- [X] i64 addition with overflow guard.
- [X] i64 comparison with invalid-result fail-closed test.
- [X] Fixnum-only =SchemeObjectExport= projection through
  =GerbilValue::is_fixnum=, =GerbilValue::as_fixnum=, and
  =GerbilValue::as_fixnum_i64=.
- [X] Scenario for fixnum fixture projection plus non-fixnum and raw
  provenance fail-closed coverage.
- [ ] Exact integer / bignum ownership strategy.
- [ ] Checked conversion rules: exact integer to =i64=, =u64=, =usize=.
- [ ] Overflow status mapping for exact integer projection.
- [ ] Scenario with boundary values: =i64::MIN=, =i64::MAX=, bignum outside
  machine range.

** Inexact numbers

- [X] Raw ABI for =f64=.
- [X] Safe =f64= projection rules for NaN, infinities, and signed zero.
- [X] Scenario for finite, NaN, infinities, signed zero, and raw provenance
  fail-closed flonum projection.
- [ ] Scenario for =f64= round-trip and comparison caveats.
- [ ] Performance receipt for numeric scalar round-trips.

** Characters

- [X] Raw ABI representation for Scheme characters.
- [X] Safe Rust =char= projection.
- [X] Unicode scalar validation and invalid-codepoint fail-closed path.
- [X] Scenario for ASCII, BMP, non-BMP, and invalid scalar values.

** Strings and UTF-8

- [X] Raw borrowed UTF-8 ABI: =GerbilBorrowedUtf8=.
- [X] Safe borrowed UTF-8 owner view: =GerbilUtf8=.
- [X] Tests for text bytes and empty string zero-length contract.
- [X] Scheme native-surface shape for borrowed UTF-8/string, aligned with Rust
  =GerbilBorrowedUtf8= and =GerbilUtf8= ownership/nullability rules.
- [ ] Owned Scheme string handle type.
- [ ] Borrowed Scheme string view with explicit lifetime/GC-root rule.
- [ ] Mutability policy: immutable string view vs mutable Scheme string.
- [ ] Encoding policy for non-UTF-8 bytevectors vs Scheme strings.
- [ ] Scenario for string allocation, borrow, and release/GC-root behavior.

** Symbols and keywords

- [ ] Raw ABI shape for Scheme symbols.
- [ ] Raw ABI shape for Gerbil keywords.
- [ ] Safe borrowed symbol name view.
- [ ] Safe interned symbol/keyword handle.
- [ ] Equality semantics: pointer identity vs name equality.
- [ ] Scenario for symbol/keyword intern and round-trip.

** Pairs and lists

- [ ] Raw ABI shape for pairs.
- [ ] Safe pair handle with =car= / =cdr= access policy.
- [ ] Nil-terminated list traversal API.
- [ ] Improper list detection.
- [ ] Lifetime/GC-root policy during traversal.
- [X] Caller-supplied pair/list/null handles remain fail-closed through
  =UntrustedRaw= provenance.
- [X] Runtime-backed pair/list/null predicates gated on runtime/export
  provenance.
- [X] Runtime-backed pair =car= / =cdr= / =pair_parts= projections gated on
  =SchemeObjectExport= provenance, while =UntrustedRaw= and =RuntimeSentinel=
  remain fail-closed.
- [ ] Recursive list traversal remains out of scope until traversal lifetime and
  GC-root policy are versioned.
- [ ] Scenario for proper list, improper list, empty list, and nested list.

** Vectors

- [ ] Raw ABI shape for vectors.
- [ ] Safe vector handle.
- [ ] Length query with =usize= overflow guard.
- [ ] Element access and mutation policy.
- [ ] Scenario for empty vector, scalar vector, nested value vector, and bounds
  failure.

** Hash tables and maps

- [ ] Decide whether hash tables are in v1 public surface.
- [ ] Raw hash table handle if included.
- [ ] Key/value iteration policy.
- [ ] Equality/hash semantics across Scheme and Rust.
- [ ] Scenario if included; otherwise document as post-v1.

** Procedures and callbacks

- [X] Raw =GerbilI64Callback= ABI type.
- [X] Safe =GerbilI64Callback= owner.
- [X] Borrowed =GerbilI64CallbackAbi= callback/context pair.
- [X] Null context and panic containment tests.
- [X] Scheme native-surface shape for i64 callback, aligned with Rust
  =GerbilI64Callback= ABI and panic-to-status behavior.
- [ ] General procedure handle type.
- [ ] Calling Scheme procedure from Rust.
- [ ] Passing Rust callbacks with captured state.
- [ ] Callback context ownership and drop policy.
- [ ] Reentrancy rules for callbacks entering Gerbil runtime.
- [ ] Scenario for callback success, callback error, panic containment, and
  lifecycle invalidation.

** Opaque values and GC rooting

- [X] Raw opaque value handle: =GerbilValueHandle=.
- [X] Safe non-zero borrowed value wrapper: =GerbilValue<'runtime>=.
- [X] Zero value handle fail-closed test.
- [X] First real =scheme-object= export fixture: Scheme null object as a
  borrowed, unrooted =GerbilValueHandle=.
- [X] Runtime-backed =null?= predicate gate for =SchemeObjectExport= handles.
- [X] Runtime-backed =pair?= and =list?= predicate gates for
  =SchemeObjectExport= handles, with pair/proper-list/improper-list fixtures.
- [X] Scheme native-surface shape for =GerbilValue= ownership and nullability:
  non-zero, runtime-borrowed, not retained beyond the valid runtime borrow.
- [ ] Explicit GC-root type.
- [ ] Scheme native-surface shape for future rooted value contract.
- [ ] Borrowed value vs owned/rooted value distinction.
- [ ] Root release/drop policy.
- [ ] Runtime-affine value marker preventing cross-runtime use.
- [ ] Scenario proving unrooted borrowed values cannot be retained.

** Bytevectors and binary buffers

- [ ] Raw ABI shape for bytevectors.
- [ ] Safe borrowed byte slice.
- [ ] Owned bytevector allocation path.
- [ ] Mutability policy.
- [ ] Zero-copy eligibility rules.
- [ ] Scenario for empty bytes, binary bytes with zeros, and large buffer.

** Ports, continuations, promises, and advanced Scheme objects

- [ ] Decide v1 scope for ports.
- [ ] Decide v1 scope for continuations.
- [ ] Decide v1 scope for promises.
- [ ] Document unsupported advanced objects with explicit status codes.

* Ownership and safety checklist

- [ ] Every public wrapper states whether it is borrowed, owned, rooted, or
  process-global.
- [ ] Every raw pointer has a nullability rule.
- [ ] Every borrowed pointer has a retention rule.
- [ ] Every callback has a panic containment rule.
- [ ] Every runtime-bound value has thread-affinity semantics.
- [ ] Every value crossing FFI has an error/status mapping.
- [ ] No public safe API exposes =unsafe extern "C"= directly.
- [ ] Unsafe blocks include narrow safety comments at the actual boundary.
- [ ] Scenario coverage exists for null, invalid value, panic, wrong thread, and
  lifecycle-finalized failures.

* Scenario and performance checklist

- [X] =status-contract= scenario.
- [X] =native-runtime-round-trip= scenario.
- [X] =native-identity-round-trip= scenario.
- [X] =native-ffi-steady-state= scenario.
- [X] =native-value-surface= scenario.
- [X] =source-surface-sync= scenario.
- [X] =backed-value-family-surface= scenario.
- [ ] =scheme-sentinel-values= scenario.
- [ ] =scheme-string-symbol-keyword= scenario.
- [ ] =scheme-pair-list-vector= scenario.
- [ ] =scheme-procedure-callback= scenario.
- [ ] =scheme-gc-rooting= scenario.
- [ ] =scheme-numeric-boundaries= scenario.
- [X] Every current required scenario has a benchmark contract.
- [ ] Each scenario has a benchmark contract with sub-millisecond target for
  pure Rust validation and explicit budget for live Gerbil/AOT paths.
- [ ] Benchmark receipts distinguish cold startup, warm runtime, and hot FFI
  steady-state.

* ASP/source-surface checklist

- [X] ASP workaround owner exists: =scheme/asp/native-surface.ss=.
- [X] Workaround projects current scalar and backed shape selectors:
  runtime/status/i64/bool/comparison/UTF-8/value-handle/i64-callback
  /native-value/native-error/native-result plus scalar exports.
- [ ] Direct provider projection for real =scheme/native.ss= FFI/export owner.
- [ ] Fix or document the current =scheme/native.ss= provider blocker:
  parser-owned search requires a provider query-pack descriptor and exact query
  currently returns no owner items.
- [ ] Selector set for every public Scheme export.
- [ ] Selector set for every Scheme type/shape.
- [X] Add Scheme selectors for Rust-supported but Scheme-unprojected shapes:
  string/UTF-8, callback, opaque value nullability, and GC/rooting policy.
- [?] CI check that ASP source-surface selectors match Rust ABI expectations is
  covered by Rust scenario tests for the ASP workaround owner; direct provider
  projection is still open.
- [ ] Remove or downgrade workaround once direct Scheme FFI/export projection is
  proven.

* 2026-07-20 audit TODO summary

** 2026-07-20 first closure receipt

- [X] Added Scheme native-surface selectors:
  =gerbil_scheme_rust_utf8_shape=, =gerbil_scheme_rust_value_handle_shape=,
  and =gerbil_scheme_rust_i64_callback_shape=.
- [X] Extended =gerbil_scheme_rust_native_value_shape= with explicit borrowed
  values, handle values, callback values, nullability, and rooting contract
  fields.
- [X] Added Rust-side source-surface synchronization coverage in
  =native_value_surface= for the new Scheme shape selectors and key contract
  tokens.
- [?] Scenario benchmark suite remains green against the current required
  scenario set; a dedicated Scheme source-surface scenario should still be
  added if we want scenario receipts to track selector drift directly.

The immediate next implementation slice should close the mismatch between the
Rust safe surface and the Scheme ASP surface:

- [X] Add =gerbil_scheme_rust_utf8_shape= to =scheme/asp/native-surface.ss=.
- [X] Add =gerbil_scheme_rust_i64_callback_shape= to
  =scheme/asp/native-surface.ss=.
- [X] Extend =gerbil_scheme_rust_native_value_shape= with explicit
  non-zero/runtime-borrowed/GC-not-rooted value semantics.
- [ ] Extend =gerbil_scheme_rust_native_result_shape= or add a new result-family
  shape that maps Rust =NativeError= and unknown status preservation.
- [X] Add tests that query Scheme shape selectors and compare them with Rust
  =sys= and safe selectors for status, UTF-8, opaque value, and callback.
- [ ] Add a scenario for Scheme source-surface synchronization so drift is
  visible in CI.

** 2026-07-20 second closure receipt

- [X] Added =gerbil_scheme_rust_native_error_shape= to project Rust
  =NativeError= taxonomy into the Scheme native source-surface.
- [X] Extended =gerbil_scheme_rust_native_result_shape= from status-only
  errors to =native-error= with optional status projection and unknown-code
  preservation.
- [X] Added =source-surface-sync= scenario manifests and raised the required
  scenario benchmark suite from 6 to 7 receipts.
- [X] Added Rust-side source-surface synchronization coverage for result/error
  taxonomy contract tokens.
- [ ] Keep nil/void/eof/unspecified, f64, char, symbol, keyword, pair, list,
  vector, hash, bytevector, port, continuation, and advanced Scheme object
  shapes open until the Rust/sys ABI has real support instead of design-only
  placeholders.

** 2026-07-20 third closure receipt

- [X] Added =backed-value-family-surface= scenario manifests and raised the
  required scenario benchmark suite from 7 to 8 receipts.
- [X] Added Rust-side source-surface synchronization coverage for the complete
  currently backed Scheme value-family selector set:
  i64, bool, comparison, borrowed UTF-8/string, opaque value handle,
  i64 callback, native value, native error, and native result.
- [X] Added an explicit negative gate proving unsupported Scheme value-family
  selectors remain absent until =gerbil-scheme-sys= and safe Rust APIs expose
  real ABI support.
- [X] Updated the evidence baseline from the stale 14-selector source-surface
  inventory to the current 18-selector ASP workaround owner.
- [ ] Next real implementation family remains sentinel or numeric expansion
  only after Rust/sys ABI support exists for nil/void/eof/unspecified or f64;
  otherwise continue reducing Marlin-local duplicated binding code against the
  current generic surface.

** 2026-07-20 fourth closure receipt

- [X] Raised =native_value_surface= from selector-presence checks to
  field-level Scheme native-shape contract checks for:
  =native-value=, =native-error=, and =native-result=.
- [X] Locked the current runtime scenario benchmark suite to the exact 8
  required scenario roots so future count-only drift cannot hide deletion or
  replacement of a critical FFI/type/performance scenario.
- [X] Confirmed every current required runtime scenario is still
  =scenario.toml=-backed.
- [X] Verified local receipts:
  =cargo check --workspace --all-targets --locked=,
  =cargo test --workspace --locked=,
  =cargo clippy --workspace --all-targets --locked -- -D warnings=, and
  =cargo test -p gerbil-scheme --features native --test native_value_surface=.
- [X] Verified remote CI receipt for commit =44d15ec=:
  Linux and macOS bindings jobs both green, including Cargo and Bazel gates.
- [X] Verified the Marlin consumer PR remains the downstream integration lane:
  Marlin PR #1 consumed the local convergence work and its CI was green before
  this checklist update.

The next implementation slice should now stop expanding design-only Scheme
shape placeholders and instead add one real sys/safe-backed value family at a
time. Each accepted family must land as raw ABI, safe wrapper, Scheme
native-surface projection, scenario benchmark, and Marlin-consumer reduction.

** 2026-07-20 fifth closure receipt

- [X] Added the public backed type matrix as a first-class ORG table covering
  the current v0 release-auditable surface.
- [X] Added =public_backed_type_matrix_covers_current_native_surface= so CI
  fails when a backed family lacks a Scheme selector, raw ABI owner, safe Rust
  surface, ownership/rooting rule, nullability rule, failure policy, or
  scenario receipt.
- [X] Included the aggregate =native-value= shape as its own release row so the
  current scalar/borrowed/handle/callback grouping cannot drift silently.
- [X] Kept unsupported value families blocked; this slice does not add
  design-only placeholders for unimplemented Scheme families.
- [X] Verified =cargo test -p gerbil-scheme --features native --test
  native_value_surface= with 10 passing tests before the runtime lifecycle and
  result/error follow-up slices.

** 2026-07-20 seventh closure receipt

- [X] Added pure Rust =NativeResult= / =NativeError= projection coverage for:
  successful native result status, known status projection, unknown status code
  preservation, =from_result= wrapping, =as_result= borrowing, and
  =into_result= ownership handoff.
- [X] Raised the release-auditable backed type matrix from 10 to 13 rows by
  adding first-class =fixnum=, =char=, and =flonum= rows instead of leaving
  accepted Scheme-object projections as implicit runtime test coverage.
- [X] Updated =gerbil_scheme_rust_native_value_shape= so aggregate scalar values
  include =fixnum=, =char=, and =flonum= alongside =i64=, =bool=,
  =comparison=, and =status=.
- [?] Local full-gate follow-up is partially verified in this turn:
  =native_value_surface= and =source_surface= pass, but the ASP-required
  =asp-testing= resident is in =Repair= due model mismatch, so
  =gerbil-scheme-sys= drift gate, workspace clippy, and Bazel must be rerun
  after the typed testing resident is repaired or by remote CI.
- [X] Kept unknown raw status codes fail-closed at the typed projection layer:
  =NativeError::Status= retains the raw code, while =status= returns =None=
  for codes outside the stable =GerbilStatus= ABI enum.
- [X] Covered non-status safe errors that intentionally map to
  =GerbilStatus::InvalidValue= and internal lifecycle errors that intentionally
  do not expose a stable status projection.
- [ ] This slice does not add a new benchmark scenario root; it closes the
  pure Rust result/error projection gap under the existing native value-surface
  test owner. Add a dedicated scenario root only when the benchmark manifest
  taxonomy is expanded deliberately.

* Next execution plan

** Phase 1: make the current v0 surface release-auditable

- [X] Add an explicit public type matrix section covering every currently
  backed shape:
  status, i64, bool return, comparison, borrowed UTF-8, opaque value handle,
  i64 callback, native value, native error, and native result.
- [X] For each backed shape, document:
  raw ABI owner, safe Rust wrapper, nullability, ownership/rooting, failure
  status, and scenario coverage.
- [X] Add a release-readiness test or harness receipt that fails if the public
  type matrix omits a currently backed shape.
- [X] Add native result/error scenario coverage that validates status,
  unknown-code preservation, optional status projection, and fail-closed
  behavior together.
- [ ] Keep unsupported families explicitly absent until raw ABI and safe Rust
  support exist; do not add Scheme source-surface placeholders for nil, void,
  f64, char, symbol, keyword, pair, list, vector, bytevector, procedure, port,
  or continuation.

** Phase 2: choose the first real new Scheme value family

- [ ] Prefer sentinel values first if the C ABI can expose them cleanly:
  nil, void, EOF, and unspecified.
- [ ] Alternative first expansion is =f64= if numeric scalar performance is the
  priority.
- [ ] Do not implement list/vector/string ownership before the GC-rooting model
  is explicit.
- [ ] For the chosen family, land all five artifacts in one slice:
  =gerbil-scheme-sys= ABI, =gerbil-scheme= safe wrapper, Scheme
  native-surface shape, scenario benchmark, and native value-surface test.
- [ ] Add failure cases first: null/invalid sentinel, invalid f64, overflow, or
  unsupported conversion depending on the selected family.

** Phase 3: FFI generation and header ownership decision

- [ ] Audit the current =cbindgen= usage and generated-header validation owner.
- [ ] Decide whether v0/v1 public C ABI is:
  handwritten-minimal, generated-by-cbindgen, or mixed with generated header
  validation only.
- [ ] If =bindgen= is considered, document why it is or is not worth adding:
  dependency cost, generated Rust stability, cross-platform reproducibility,
  auditability of =unsafe extern "C"= declarations, and Bazel/Cargo cache
  impact.
- [ ] Keep =unsafe extern "C"= contained in =gerbil-scheme-sys=; safe crates
  must expose typed wrappers and status-preserving errors, not raw externs.

** Phase 4: scenario and performance expansion

- [ ] Split scenario budgets by lane:
  pure Rust validation, warm native ABI call, real Gerbil toolchain execution,
  and AOT/native build path.
- [ ] Keep pure Rust validation in sub-millisecond territory.
- [ ] Track live Gerbil/AOT paths separately; do not let cold startup dominate
  hot FFI steady-state receipts.
- [ ] Add scenario receipts for the next accepted family and one invalid/failure
  case per family.
- [ ] Keep the explicit 8-root scenario suite list updated deliberately when a
  new required scenario is added.

** Phase 5: Marlin convergence and dependency boundary

- [ ] Treat Marlin as downstream consumer, not as the owner of this crate's API.
- [ ] In Marlin, replace duplicated Gerbil binding code with
  =gerbil-scheme-rust= dependency usage only after the matching generic wrapper
  exists here.
- [ ] Keep Marlin-specific package inventory, policy packs, and storage receipts
  in Marlin.
- [ ] Add a downstream integration receipt proving Marlin consumes this crate
  without reintroducing raw FFI or local duplicated binding catalogs.
- [ ] Backport only generic lessons from Marlin, such as loadpath/toolchain
  handling or scenario harness behavior; avoid importing Marlin domain objects.

* Marlin convergence checklist

- [ ] Keep this crate generic; do not encode Marlin package inventory policy.
- [ ] Map Marlin Gerbil package types to this crate's public wrappers.
- [ ] Replace Marlin-local raw FFI uses with =gerbil-scheme= safe API.
- [ ] Keep Marlin-specific tests in Marlin; keep reusable Scheme/Rust binding
  tests here.
- [ ] Add a downstream integration receipt showing Marlin consumes this crate as
  a dependency without duplicating binding code.

* Release readiness checklist

- [ ] Public type matrix is documented.
- [ ] All accepted v1 types have raw ABI, safe wrapper, tests, and scenario.
- [ ] Unsafe API surface is audited and minimized.
- [ ] License headers and dual-license policy are confirmed.
- [ ] Version remains pre-release until v1 API boundary is intentionally cut.
- [ ] CI green across Linux and macOS.
- [ ] CHANGELOG/RFC entry records first supported binding surface.
