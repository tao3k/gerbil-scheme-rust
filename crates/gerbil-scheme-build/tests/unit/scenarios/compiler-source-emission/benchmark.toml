harness = "libtest"
test = "compiler_source_emission_stays_within_scenario_budget"
phase = "warm"
target_total = "5ms"
max_total = "100ms"
observed_total = "5ms"
regression_budget = "50ms"
memory_budget_bytes = 16777216
observed_memory_bytes = 0
target_rationale = "Opaque source validation and file emission are Rust-owned hot paths; external gxc startup remains a separate correctness and toolchain receipt."

[observed_timings]
emit_source = "5ms"
