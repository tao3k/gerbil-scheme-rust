harness = "libtest"
test = "borrowed_collection_surfaces_preserve_pointer_and_length_contracts"
phase = "warm"
observed_total = "100us"
target_total = "1ms"
target_rationale = "Borrowed collection ABI shape checks only validate pointer and length projection, so the target is sub-millisecond."
max_total = "500ms"
regression_budget = "10ms"
observed_memory_bytes = 0
memory_budget_bytes = 16777216

[observed_timings]
borrowed_collection_shape = "100us"
borrowed_collection_shape_budget = "1ms"
