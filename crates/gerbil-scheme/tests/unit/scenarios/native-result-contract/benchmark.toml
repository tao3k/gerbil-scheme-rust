harness = "libtest"
test = "native_result_projects_success_and_error_statuses"
target_total = "750us"
max_total = "2ms"
observed_total = "500us"
regression_budget = "250us"
memory_budget_bytes = 1048576
observed_memory_bytes = 0
target_rationale = "NativeResult status projection is a pure Rust wrapper check and should remain sub-millisecond; the 2ms max keeps CI noise from hiding real regressions."
observed_timings = { total = "500us", p50 = "250us", p95 = "500us", p99 = "750us", status_projection = "250us" }
