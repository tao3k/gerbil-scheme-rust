harness = "libtest"
test = "scheme_borrowed_vector_preserves_handle_slice_and_abi_projection"
phase = "warm"
observed_total = "100us"
target_total = "1ms"
target_rationale = "Safe vector projection only preserves a borrowed handle slice pointer and length."
max_total = "500ms"
regression_budget = "10ms"
observed_memory_bytes = 0
memory_budget_bytes = 16777216

[observed_timings]
safe_vector_borrow = "100us"
safe_vector_borrow_budget = "1ms"
