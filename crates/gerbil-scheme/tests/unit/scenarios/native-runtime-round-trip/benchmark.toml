harness = "libtest"
test = "initialized_runtime_crosses_the_live_gerbil_abi"
phase = "warm"
target_total = "100ms"
max_total = "500ms"
observed_total = "100ms"
regression_budget = "250ms"
memory_budget_bytes = 268435456
observed_memory_bytes = 0
target_rationale = "The warm runtime path performs one initialization, ABI negotiation, and 10000 scalar calls without allocation ownership transfer."

[observed_timings]
runtime_round_trip = "100ms"
