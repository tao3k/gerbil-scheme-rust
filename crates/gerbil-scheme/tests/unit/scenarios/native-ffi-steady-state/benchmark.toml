harness = "libtest"
test = "steady_state_native_ffi_reports_statistical_receipt"
phase = "warm"
target_total = "100ms"
max_total = "500ms"
observed_total = "60ms"
regression_budget = "100ms"
memory_budget_bytes = 268435456
observed_memory_bytes = 0
target_rationale = "The sub-second hard gate initializes Gerbil once, then records 100 samples of 250 add, predicate, and comparison triplets through the safe non-Send runtime handle."

[observed_timings]
median_scalar_triplet = "3us"
median_scalar_triplet_budget = "5us"
